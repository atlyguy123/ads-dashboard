# Virtual environment
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Data files (will be created on Railway)
data/
# Keep database directory structure but exclude large DB files from uploads
database/*.db
database/*.sqlite
database/*.sqlite3
database/*copy.db
database/*.backup
# Keep schema files
!database/schema.sql
!database/*.sql

# Development files
.DS_Store
.vscode/
.idea/
*.log

# Node modules (keep source files for build process)
orchestrator/dashboard/client/node_modules/
# Note: src/ and public/ are needed for React build process
# orchestrator/dashboard/client/tailwind.config.js
# orchestrator/dashboard/client/postcss.config.js

# Build artifacts (exclude build tools, keep final builds)
*.egg-info/
build/
dist/

# Large analysis files (but keep React build files)
data/*.json
!requirements.txt
!package.json
!railway.json

# Git
.git/ 